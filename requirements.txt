# Crucible — Python dependencies
# Pin to known-good versions. Update deliberately.

# Core framework
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3

# AI
anthropic==0.40.0

# Async HTTP (research adapters)
httpx==0.28.1

# Scheduling (APScheduler for data feed agent + daily snapshots)
apscheduler==3.10.4

# Database
sqlalchemy==2.0.36
asyncpg==0.30.0          # async PostgreSQL driver

# Cache
redis==5.2.1

# Data / research adapters
pandas==2.2.3            # FRED / World Bank data wrangling
feedparser==6.0.11       # RSS news adapter

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0

# PDF generation
markdown==3.7
xhtml2pdf==0.2.16

# Dev utilities
python-dotenv==1.0.1     # .env loading for local dev
